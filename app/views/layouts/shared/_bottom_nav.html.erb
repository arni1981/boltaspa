<nav class="fixed bottom-0 left-0 right-0 bg-[#0a1d13]/95 backdrop-blur-md border-t border-white/5 z-50 pt-3 pb-[calc(0.75rem+env(safe-area-inset-bottom))]">
  <div class="max-w-2xl mx-auto px-10 flex items-center justify-between">

    <%= nav_link_to t('.bottom_nav.home'), root_path %>
    <%= nav_link_to t('.bottom_nav.predictions'), predictions_path %>
    <%= nav_link_to t('.bottom_nav.leagues'), leagues_path %>

  </div>
</nav>
